wifi:
  ssid: !secret wifi_ssid
  password: !secret wifi_password
  ap:
    ssid: ${device_name} Fallback Hotspot
    password: !secret wifi_password

# Enables captive portal in fallback mode.
captive_portal: